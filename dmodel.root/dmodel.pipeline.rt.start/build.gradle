plugins {
	id 'org.springframework.boot' version '2.1.2.RELEASE'
}

apply plugin: 'org.springframework.boot'

dependencies {
	compile project(":dmodel.pipeline.rt.validation")
	compile project(":dmodel.pipeline.rt.entry")
}

